packages:
  # all packages in direct subdirs of packages/ 我负责的所有的项目存储地方
  - 'packages/*'
  # all packages in subdirs of components/ 给我所有的项目使用的公开组件
  - 'components/**'
  # exclude packages that are inside test directories 过滤掉的文件
  - '!**/test/**'
  # 公共的方法
  - 'api/**'